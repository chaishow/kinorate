from django.utils.translation import gettext_lazy as _

EMPTY_FIELDS = _('Fill up all fields')
WRONG_DATA = _('Wrong login or password')
ALREADY_EXISTS = _('User with this email or login already exists')
